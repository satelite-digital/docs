const fetchFilesTest = require('./fetchFiles.spec')
const parseCommentsTest = require('./parseComments.spec')
const createTreeTest = require('./createTree.spec')

module.exports = {
    fetchFilesTest,
    parseCommentsTest,
    createTreeTest
}